# Database connection used by SQLAlchemy (used inside the container)
# Host `db` matches the docker-compose service name. If your root password
# contains special characters (e.g. '#'), URL-encode them (e.g. %23).
DATABASE_URL=mysql+pymysql://root:MimoTeck1793@db:3306/fastapi_todoapp

# MySQL settings for the db service (kept here for convenience)
MYSQL_ROOT_PASSWORD=MimoTeck1793
MYSQL_DATABASE=fastapi_todoapp
